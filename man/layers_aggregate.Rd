% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{layers_aggregate}
\alias{layers_aggregate}
\title{Aggregate layers in a stack or matrix}
\usage{
layers_aggregate(
  dat,
  layer_ids = NULL,
  drivers = NULL,
  vcs = NULL,
  by = c("both", "drivers", "vcs", "none"),
  fun = c("sum", "mean", "median", "min", "max", "sd"),
  exportAs = c("SpatRaster", "matrix")
)
}
\arguments{
\item{dat}{SpatRaster, matrix (with \code{template} attribute), or data.frame.}

\item{layer_ids}{Optional exact layer names to keep before aggregating.}

\item{drivers}{Optional driver names to keep (matches suffix after \verb{_}).}

\item{vcs}{Optional valued component names to keep (matches prefix before \verb{_}).}

\item{by}{Aggregation mode: "both" (single layer), "drivers" (aggregate over drivers per VC),
"vcs" (aggregate over VCs per driver), or "none" (no aggregation).}

\item{fun}{Aggregation function: "sum", "mean", "median", "min", "max", "sd".}

\item{exportAs}{string, "SpatRaster" or "matrix".}
}
\value{
Aggregated object (SpatRaster by default).
}
\description{
Aggregate layers in a stack or matrix
}
\examples{
drv_paths <- system.file(
  "extdata/rasters",
  c("pressure_shipping.tif", "pressure_climate.tif"),
  package = "rcea"
)
vc_paths <- system.file(
  "extdata/rasters",
  c("vc_cod.tif", "vc_salmon.tif"),
  package = "rcea"
)
drivers <- terra::rast(drv_paths); names(drivers) <- c("shipping", "climate")
vc <- terra::rast(vc_paths); names(vc) <- c("cod", "salmon")
sens <- matrix(
  c(0.8, 0.5,
    0.2, 0.7),
  nrow = 2,
  dimnames = list(c("cod", "salmon"), c("shipping", "climate"))
)
ce <- cea(drivers, vc, sens, exportAs = "SpatRaster")
# Aggregate all selected layers to a single layer
layers_aggregate(ce, by = "both")
# Aggregate over drivers (one layer per VC)
layers_aggregate(ce, by = "drivers")
# Use a different aggregation function
layers_aggregate(ce, by = "drivers", fun = "mean")

}
