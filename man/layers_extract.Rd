% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{layers_extract}
\alias{layers_extract}
\title{Extract layers from a stack or matrix}
\usage{
layers_extract(dat, layer_ids = NULL, drivers = NULL, vcs = NULL)
}
\arguments{
\item{dat}{SpatRaster, matrix (with \code{template} attribute), or data.frame.}

\item{layer_ids}{Optional exact layer names to keep.}

\item{drivers}{Optional driver names to keep (matches suffix after \verb{_}).}

\item{vcs}{Optional valued component names to keep (matches prefix before \verb{_}).}
}
\value{
Same type as input, with selected layers.
}
\description{
Extract layers from a stack or matrix
}
\examples{
drv_paths <- system.file(
  "extdata/rasters",
  c("pressure_shipping.tif", "pressure_climate.tif"),
  package = "rcea"
)
vc_paths <- system.file(
  "extdata/rasters",
  c("vc_cod.tif", "vc_salmon.tif"),
  package = "rcea"
)
drivers <- terra::rast(drv_paths)
names(drivers) <- c("shipping", "climate")
vc <- terra::rast(vc_paths)
names(vc) <- c("cod", "salmon")
sens <- matrix(
  c(
    0.8, 0.5,
    0.2, 0.7
  ),
  nrow = 2,
  dimnames = list(c("cod", "salmon"), c("shipping", "climate"))
)
ce <- cea(drivers, vc, sens, exportAs = "SpatRaster")
# Keep only the shipping driver layers
layers_extract(ce, drivers = "shipping")

}
