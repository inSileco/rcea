% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{layers_per_area}
\alias{layers_per_area}
\title{Compute per-area effects for layered outputs}
\usage{
layers_per_area(
  dat,
  vc,
  drivers = NULL,
  vcs = NULL,
  layer_ids = NULL,
  area_unit = "km"
)
}
\arguments{
\item{dat}{SpatRaster or matrix (with \code{template} attribute) containing layers
named as \code{vc_driver}.}

\item{vc}{SpatRaster or matrix (with \code{template} attribute) of valued components.}

\item{drivers}{Optional driver names to keep (matches suffix after \verb{_}).}

\item{vcs}{Optional valued component names to keep (matches prefix before \verb{_}).}

\item{layer_ids}{Optional exact layer names to keep before aggregation.}

\item{area_unit}{Unit for area calculation passed to \code{terra::cellSize()} (e.g., "km").}
}
\value{
data.frame with one row per VC and one column per driver.
}
\description{
Calculates per-unit-area values for each driver/VC layer using an area mask
derived from \code{vc}.
}
\examples{
drv_paths <- system.file(
  "extdata/rasters",
  c("pressure_shipping.tif", "pressure_climate.tif"),
  package = "rcea"
)
vc_paths <- system.file(
  "extdata/rasters",
  c("vc_cod.tif", "vc_salmon.tif"),
  package = "rcea"
)
drivers <- terra::rast(drv_paths); names(drivers) <- c("shipping", "climate")
vc <- terra::rast(vc_paths); names(vc) <- c("cod", "salmon")
sens <- matrix(
  c(0.8, 0.5,
    0.2, 0.7),
  nrow = 2,
  dimnames = list(c("cod", "salmon"), c("shipping", "climate"))
)
ce <- cea(drivers, vc, sens, exportAs = "SpatRaster")
layers_per_area(ce, vc)

}
