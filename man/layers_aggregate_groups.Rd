% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers.R
\name{layers_aggregate_groups}
\alias{layers_aggregate_groups}
\title{Aggregate layers with group balancing}
\usage{
layers_aggregate_groups(
  dat,
  group_col = "driver_group",
  time_col = NULL,
  keep_vc = TRUE,
  within_fun = c("mean", "sum", "median", "min", "max", "sd"),
  across_fun = c("sum", "mean", "median", "min", "max", "sd"),
  group_weights = NULL,
  exportAs = c("SpatRaster", "matrix")
)
}
\arguments{
\item{dat}{SpatRaster, matrix (with \code{template} attribute), or rcea_result.}

\item{group_col}{Grouping column in \code{layer_map} used for balancing (e.g., \code{driver_group}).}

\item{time_col}{Optional time column in \code{layer_map} (e.g., \code{driver_month}) to keep
temporal slices separate during both aggregation stages.}

\item{keep_vc}{Logical; if TRUE (default), aggregation is performed separately per VC.}

\item{within_fun}{Aggregation function within each group.}

\item{across_fun}{Aggregation function across groups.}

\item{group_weights}{Optional named numeric vector of group weights applied in
the across-group stage. Names must match values in \code{group_col}.}

\item{exportAs}{string, "SpatRaster" or "matrix".}
}
\value{
Aggregated object (SpatRaster by default).
}
\description{
Performs two-stage aggregation to avoid bias from groups with more member
layers:
\enumerate{
\item aggregate within groups (e.g., mean across shipping categories),
\item aggregate across groups (e.g., sum shipping + fishing).
}
}
